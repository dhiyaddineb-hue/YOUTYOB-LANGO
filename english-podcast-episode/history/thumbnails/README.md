# Thumbnail CTR history

Canonical file: `../../data/thumbnail-history.json`

The studio appends every analysis to that file (via download) and to `localStorage`. Compare variants over time before replacing a live thumbnail.
